# dither field

interactive dithered noise field. move your mouse — the dithering flows away around the cursor.

bayer 4×4 ordered dithering over animated sine noise. pure canvas, no dependencies.

## run

open `index.html` in a browser.
